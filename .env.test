# Environment variables for Vitest test runs.
# These values are intentionally fake — they must be non-empty so that
# src/config/env.ts does not throw during module initialisation in tests.
VITE_GOOGLE_CLIENT_ID=test-google-client-id
VITE_AUTH_BASE_URL=http://localhost:3000/auth
